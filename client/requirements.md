## Packages
framer-motion | For smooth animations of navigation elements and transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
The application relies on CSS scroll snapping for the main presentation flow.
Slides are loaded via iframes pointing to static HTML files served from the public directory.
Backend provides the list of slides and their order.
